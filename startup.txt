git clone https://github.com/THU-MIG/yolov10.git
cd yolov10
pip install -r requirements.txt
pip install -e .

cd ..
pip install -r requirements.txt

gunicorn --bind=0.0.0.0 --worker-class uvicorn.workers.UvicornWorker main:app
